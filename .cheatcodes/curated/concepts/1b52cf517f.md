---
cheatcodes_id: 1b52cf517f
type: Decision
title: Allow optional execution data in terminal snapshots
description: Terminal snapshots may include an optional completed or aborted execution field while remaining compatible with snapshot parsing, latest-snapshot lookup, and reload behavior.
tags:
  - snapshots
  - backward-compatibility
  - runtime
status: draft
generated:
  by: cheatcodes/0.2.0
  at: 2026-08-29T20:16:36.645Z
sources:
  - id: session-32c70bd411becc58
    resource: session:01a0439b-cd7c-7ccb-9298-bba5301c6f75#entries=237f71ff
    title: Session evidence
---

# Answer

Keep terminal snapshot parsing compatible with optional embedded completed or aborted execution data.

# Why

A 4/4 throwaway node:test probe passed: parseSnapshot returned terminal status for terminal variants with and without execution, latestSnapshot parsed each variant as terminal, and the fresh coordinator reload probe covered staying idle.

# Evidence

- [evidence-9b4989fa4dc15aaf441a781f] Validation failed for tool "workflow_transition":
  - checkpoint: must not have additional properties

Received arguments:
{
  "checkpoint": {
    "data": {
      "claim": "Adding an optional execution field to terminal snapshots stays backward compatible with parseSnapshot and with reload-stays-idle behavior.",
      "method": "Throwaway node:test probe at .pi-files/probe-terminal-compat.test.mjs: parseSnapshot on terminal {v:5,status,workflow,runId} with and without an embedded completed/aborted execution; latestSnapshot on a branch ending in each variant; a fresh RuntimeCoordinator running handleSessionStart on a harness branch whose last choreograph entry is each terminal variant; plus an active-snapshot strictness sanity leg.",
      "observation": "4/4 probe tests pass. parseSnapshot returns {status:\"terminal\"} for all terminal variants including those carrying execution (it checks completed/aborted before any other field). latestSnapshot parses each as terminal. A fresh coordi
[truncated]
